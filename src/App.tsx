import "@picocss/pico";

const App = () => {
    return (
        <div className="container">
            <h2>Exciting New Cocktail application!</h2>
            <p>index.tsx is the entry point</p>
            <p>App.tsx sets up the auth and routing</p>
        </div>
    );
};

export default App;
