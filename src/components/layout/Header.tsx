import NavBar from "components/navigation/NavBar";

const HeaderBar = () => {
    return (
        <header>
            <h1>CocktailZ</h1>
            <NavBar />
        </header>
    );
};

export default HeaderBar;
